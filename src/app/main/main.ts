import { Component } from '@angular/core';
import { Layout, LibLayoutFooter } from "@univpm/layout";

@Component({
  selector: 'app-main',
  imports: [Layout, LibLayoutFooter],
  templateUrl: './main.html',
  styleUrl: './main.scss'
})
export class Main {

}
